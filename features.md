# ポモドーロタイマー 実装機能一覧

このドキュメントは、ポモドーロタイマー Webアプリケーションの実装対象機能を整理したものです。

## 1. 必須機能（MVP）

### 1.1 タイマー基本操作

- 作業タイマーの開始
- 一時停止 / 再開
- リセット
- 残り時間表示（mm:ss）

### 1.2 セッションモード管理

- 作業モード
- 短休憩モード
- 長休憩モード
- 作業完了後の自動モード遷移

### 1.3 進捗表示

- 円形プログレス表示（経過率）
- 今日の完了ポモドーロ数
- 今日の集中時間（合計）

### 1.4 状態保持

- ページリロード後の状態復元（実行中/停止中、残り時間、モード）
- セッション完了情報の保存

### 1.5 バックエンドAPI（最小）

- 設定取得API
- 設定更新API
- セッション完了記録API
- 今日の統計取得API

### 1.6 永続化（最小）

- 設定値の保存
- セッション履歴の保存
- 当日統計の取得

## 2. 推奨機能（実用化）

### 2.1 設定カスタマイズ

- 作業時間
- 短休憩時間
- 長休憩時間
- 長休憩に入る間隔（例: 4セットごと）

### 2.2 通知機能

- セッション終了時の通知音
- ブラウザ通知（許可時）

### 2.3 UX改善

- 次モードの予告表示
- ボタン状態制御（実行中は開始不可など）
- モバイル対応レイアウト

### 2.4 例外/異常系対応

- API失敗時のリトライまたはエラーメッセージ表示
- 不正設定値のバリデーション
- タイマー二重起動防止

## 3. テスト観点として実装すべき機能単位

### 3.1 時間計算ロジック

- 残り秒数計算
- セッション終了判定
- ドリフト耐性（deadline基準）

### 3.2 状態遷移ロジック

- 作業 -> 短休憩
- 規定回数後の長休憩
- リセット時の初期化

### 3.3 集計ロジック

- 今日の完了数
- 今日の集中時間
- 日付境界の扱い

## 4. 非機能要件（最低限）

### 4.1 パフォーマンス

- 1秒ごとのUI更新でカクつかない
- 軽量な初期ロード

### 4.2 保守性

- UI / ロジック / API / DB の責務分離
- 設定値・定数の一元管理

### 4.3 信頼性

- リロードやタブ復帰時にタイマー状態が破綻しない
