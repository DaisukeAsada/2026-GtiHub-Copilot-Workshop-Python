# ポモドーロタイマー 段階的実装計画

このドキュメントは、UIモック、アーキテクチャ方針、機能一覧を踏まえた段階的な実装計画をまとめたものです。

## 1. 実装粒度の基本方針

- 1フェーズは 1〜3日で完了できる規模にする
- 各フェーズで必ず「動く画面」と「受け入れ条件」を持つ
- ロジックを先に分離し、UI結合は薄く保つ
- テストは各フェーズで少しずつ追加し、最後にまとめない

## 2. フェーズ別計画

### Phase 1: 画面骨格と静的UI

#### 実装対象

- HTML/CSS による基本レイアウト構築
- タイマー表示、円形プログレス、操作ボタン、進捗カードの静的表示
- モック準拠のスタイリング（デスクトップ/モバイル）

#### 受け入れ条件

- モックと同等の視覚構成で表示される
- 主要UI要素が崩れずに表示される

#### テスト観点

- レスポンシブ表示確認
- UI要素の存在確認

---

### Phase 2: タイマー最小動作

#### 実装対象

- 開始
- 一時停止 / 再開
- リセット
- 残り時間（mm:ss）更新
- 円形プログレス更新

#### 実装方針

- deadline基準で残時間を再計算する方式を採用

#### 受け入れ条件

- 25:00 から開始し、停止/再開/リセットが正しく動作する
- 0秒到達でセッション完了が検知される

#### テスト観点

- 時間計算ロジックの単体テスト（境界値含む）

---

### Phase 3: モード遷移導入

#### 実装対象

- 作業モード
- 短休憩モード
- 長休憩モード
- セットカウント
- 自動遷移

#### 実装方針

- ステートマシンとして遷移ルールを明示
- 遷移判定は純粋関数化

#### 受け入れ条件

- 作業完了後に短休憩へ遷移する
- 規定回数ごとに長休憩へ遷移する
- リセットで初期状態へ戻る

#### テスト観点

- 状態遷移テスト（通常遷移/規定回数/リセット）

---

### Phase 4: 状態復元とローカル永続化

#### 実装対象

- localStorage への状態保存
- ページ再読み込み時の復元
- 実行中セッションの残時間再計算

#### 受け入れ条件

- リロード後にモード、残時間、進捗が復元される
- 期限切れ状態が破綻せず完了扱いになる

#### テスト観点

- 復元ロジックの単体テスト
- 実行中/停止中それぞれの復元テスト

---

### Phase 5: Flask API最小実装

#### 実装対象

- GET /api/settings
- PUT /api/settings
- POST /api/sessions/complete
- GET /api/stats/today

#### 受け入れ条件

- フロントから設定取得・更新できる
- セッション完了記録がAPI経由で送信できる
- 当日統計が取得・表示できる

#### テスト観点

- API正常系/異常系テスト
- 入力値バリデーションテスト

---

### Phase 6: SQLite連携と集計

#### 実装対象

- settings/sessions テーブル作成
- Repository層の実装
- Service層での日次集計

#### 受け入れ条件

- アプリ再起動後も設定と履歴が保持される
- 当日統計値が正しく算出される

#### テスト観点

- Repository統合テスト
- 日付境界（0時跨ぎ）テスト

---

### Phase 7: 実用機能と品質仕上げ

#### 実装対象

- 設定カスタマイズUI
- 通知音
- ブラウザ通知
- API失敗時のエラーハンドリング
- タイマー二重起動防止

#### 受け入れ条件

- 推奨機能の主要項目が安定動作する
- 主要異常系でアプリが破綻しない

#### テスト観点

- 通知許可なしケース
- API失敗時ケース
- 連続クリック時の安全性

## 3. フェーズ共通の成果物

各フェーズで以下を必ず残す。

- 動作確認可能な実装
- 受け入れ条件の確認結果
- 追加したテスト
- 次フェーズへの課題メモ

## 4. 実装順序の意図

- 先にUI体験を確定し、方向性のズレを早期に発見する
- 早い段階で時間計算と遷移ロジックを安定化する
- 永続化/APIは後段で段階的に複雑性を上げる
- 各段階でテストを積み上げ、手戻りを減らす
